//Routing
// install react-router-dom
//BrowserRouter, Route, Routes, Link, NavLink, useParams, useNavigate

//Dynamic Routing
// useParams, useNavigate
//what is dynamic routing?
//Dynamic routing is a technique in web development where the application can respond to different URLs by rendering different components or content. This is often achieved using libraries like React Router, which allows developers to define routes in their application and navigate between them based on user interactions or application state.

function Contact() {
  return (
    <div>
      <h1>Contact</h1>
    </div>
  );
}
export default Contact;